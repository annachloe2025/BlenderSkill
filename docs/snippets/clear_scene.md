# clear_scene.py

シーンの全オブジェクト（メッシュ・ライト・カメラ含む）を削除する最短コード。

## コード

```python
--8<-- "snippets/clear_scene.py"
```

## 使いどころ

- 学習セッションの開始時、デフォルトの Cube/Light/Camera をリセットしてから始めたいとき
- 別のスニペットを試す前にクリーンな状態を作りたいとき

## 注意点

- **編集モードでは効かない**。Object Mode で実行する前提。
- コレクション自体は残るので、空のコレクションを掃除したい場合は `bpy.data.collections.remove(...)` を別途使う。
